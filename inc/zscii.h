#pragma once

#include <cstddef>
#include <vector>

namespace machina::zmachine
{
    std::vector<std::byte> encode(const std::string& s)
    {

    }

    std::string decode(const std::vector<std::byte>& zscii)
    {

    }
}